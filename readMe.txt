1) Install Nodejs server

2) Install Mongodb
    a) Create a database with any name
    b) Create two collections with name customers and counters

3) Download the node js code from Git
    a) In config.js file set mongoDbURI attribute to your mongodb connection string
    b) In project directory run command "sudo npm install"
    c) Now run command "sudo node app.js"
